<p>This is an automatic update from ublog. Visit
 <a href="http://ublog.#{DOMAIN}.com/"> ublog </a>to check and respond to messages.</p>
 
<div>
<table>
<% for blog in @blogs do %>
  <% if blog.origin %>
  <tr>
    <td>
      <%= "<b>#{blog.origin.ublog_name}</b> #{blog.webify_http} ---" %>
      <% created_time = blog.created_at.localtime -%>
      <% elapsed = (Time.now - created_time).to_i -%>
      <% if elapsed < (12 * 3600) -%>
        <% if elapsed < (2 * 3600) -%>
          <% if elapsed < (2 * 60) -%>
            <span class="time-stamp"><%= elapsed.to_s + " seconds ago" %>
          <% else -%>
            <span class="time-stamp"><%= (elapsed/60).to_s + " minutes ago" %>
          <% end -%>
        <% else -%>
          <span class="time-stamp"><%= (elapsed/3600).to_s + " hours ago" %>
        <% end -%>
      <% else -%>
        <span class="time-stamp"><%= created_time.to_s[/^.*?[0-9]+ /] %></span>
      <% end -%>
      <%= '<span class="time-stamp"> From ' + h(blog.source) + " </span>" unless blog.source.blank? %>
    </td>
  </tr>
  <% end %>
<% end %>
</table>
</div>
<p>
<%=  'ublog is described in the <a href="http://#{DOMAIN}pedia.#{DOMAIN}.com/display/cpda/ublog">ublog user guide</a>' %><br />
<%= "Visit the Subs link on your" + '<a href="http://ublog.#{DOMAIN}.com/"> ublog </a>' + "page to unsubscribe from this email notification." %>
</p>

