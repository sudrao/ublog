<h1>New tag</h1>

<% form_for(@tag) do |f| %>
  <%= render "shared/error_messages", :target => @tag %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', tags_path %>
