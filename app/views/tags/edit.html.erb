<h1>Editing tag</h1>

<% form_for(@tag) do |f| %>
  <%= render "shared/error_messages", :target => @tag %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @tag %> |
<%= link_to 'Back', tags_path %>
